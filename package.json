{
  "name": "api-workers",
  "version": "1.0.0",
  "description": "Aviation Management API on Cloudflare Workers",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "esbuild src/worker.ts --bundle --outfile=dist/worker.js --format=esm",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.3.3",
    "wrangler": "^4.63.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
