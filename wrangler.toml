name = "api-workers"
main = "src/worker.ts"
compatibility_date = "2024-01-01"

preview_urls = true


# ============= KV NAMESPACE =============
[[kv_namespaces]]
binding = "CACHE_KV"
id = "83a861dc2e27486e86eb23909fbb9861"

# ============= OBSERVABILITY =============
[observability]
enabled = true

# ── Novo: R2 para armazenar PDFs, boletos, NFs, comprovantes ────────────────
[[r2_buckets]]
binding = "FILES"
bucket_name = "nf-boletos-clients"

# ── Novo: D1 para templates de email, log de envios e short links ──────────
[[d1_databases]]
binding = "DB"
database_name = "share-brasil-aux"
database_id = "b7048c06-cc46-4832-bac5-641a24ce245c"

# ── Secrets
EMAIL_FROM = "Financeiro Share Brasil <financeiro@share-brasil.com>"
# ── Secrets (não colocar aqui — usar `wrangler secret put`) ─────────────────
# AISWEB_API_KEY
# AISWEB_API_PASS
# RESEND_API_KEY
# INTERNAL_TOKEN
# SUPABASE_URL
# SUPABASE_ANON_KEY
# WINSOCK_VALUATION_URL
# WINSOCK_API_KEY
# WINSOCK_AUTH_HEADER (opcional; padrão: Authorization)
# WINSOCK_AUTH_PREFIX (opcional; padrão: Bearer )

# ── Cron para pré-aquecer cache (já existente) ──────────────────────────────
[triggers]
crons = ["*/15 * * * *"]
